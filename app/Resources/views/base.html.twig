<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('./css/style.css')}}"/>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    
        {% block body %}
            <body>
                {# On cree un block nav qui est rappelé dans nos autre pages#}
                {% block nav %}
                    <nav>
                        <ul>
                            {# cette barre nav ammène vers toutes les pages
                            avec path et le nom de la route (name="")#}
                            <li><a href=" {{ path('jaune')}}">jaune</a></li>
                            <li><a href=" {{ path('rouge')}}">rouge</a></li>
                            <li><a href=" {{ path('vert')}}">vert</a></li>
                            <li><a href=" {{ path('bleu')}}">bleu</a></li>
                        </ul>
                    </nav>
        {% endblock %}
            </body>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    
</html>
